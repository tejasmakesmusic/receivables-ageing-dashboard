"""Route modules — one per resource group. Empty until milestones add them.

Planned (per spec §10):
  auth.py          — /auth/google/callback, /auth/logout, /auth/me
  snapshots.py     — upload + staging + publish
  config.py        — credit-period, aliases, fx-rates, exception-buckets
  dashboard.py     — /dashboard, /parties, /invoices
  exceptions.py    — /invoices/:id/exceptions
  follow_ups.py    — /follow-ups
  reconciliation.py— /reconciliation
  admin.py         — /admin/users, /admin/audit-log, /admin/email-rules
"""
